## Why

<!-- 说明变更动机：解决什么问题？为什么现在要做？ -->

## What Changes

<!-- 描述会发生什么变化。明确新增、修改或移除的能力；破坏性变更标记 **BREAKING**。 -->

## Capabilities

### New Capabilities
<!-- 新增 capability。将 <name> 替换为 kebab-case 标识符（例如 user-auth、data-export、api-rate-limiting）。每一项会创建 specs/<name>/spec.md -->
- `<name>`: <brief description of what this capability covers>

### Modified Capabilities
<!-- REQUIREMENTS 会变化的现有 capability（不是纯实现变化）。
     只有 spec-level 行为变化才列在这里。每一项都需要 delta spec file。
     使用 openspec/specs/ 中已有 spec 名称。没有 requirement 变化时留空。 -->
- `<existing-name>`: <what requirement is changing>

## Architecture Impact

<!-- 架构影响初筛：列出需要复用或影响的现有 specs、模块、接口、共享能力、数据模型、配置、部署或测试模式。无显著架构影响时一句话说明即可。详细技术方案放入 design.md。 -->

## Impact

<!-- 受影响的代码、API、依赖或系统 -->
