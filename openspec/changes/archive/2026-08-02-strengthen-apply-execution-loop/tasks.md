## 1. Baseline and Contract

- [x] 1.1 Validate the change artifacts and confirm the generated Skill, command, schema, test, and release surfaces.

## 2. Apply Workflow Guidance

- [x] 2.1 Strengthen the shared apply body with verified task checkpoints, plan re-reading, recurring sub-agent delegation, bounded confirmation options, and default repository closeout.
- [x] 2.2 Align spec-driven and workspace-planning schema apply instructions with the same execution contract.

## 3. Verification

- [x] 3.1 Add focused regression assertions for the new generated apply contract and schema instruction output.
- [x] 3.2 Run focused tests, full tests, lint, build, strict OpenSpec validation, package-version checks, and package smoke checks.

  Verification note: the focused change is strict-valid and all 89 test files (1662 tests) pass. Repository-wide strict validation still reports six unrelated pre-existing planning changes; this change does not modify them.

## 4. Release Readiness

- [x] 4.1 Update necessary release documentation and keep package metadata and lockfiles consistent for version 1.5.0.
- [x] 4.2 Rebuild and replace the local global OpenSpec installation, then verify the installed version and generated apply Skill content.
- [x] 4.3 Synchronize apply metadata, onboarding guidance, and residual workflow documentation with the default closeout contract, then rerun focused regression tests.
